name: quick_task
description: Flutter based ToDo app.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.1.1

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0
  cupertino_icons: ^1.0.6
  parse_server_sdk_flutter: 7.0.1
  http: 0.13.6
  intl: 0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

# Following section is specific to Flutter.
flutter:
  uses-material-design: true

  assets:
    - assets/
